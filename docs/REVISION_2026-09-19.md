# 2026-09-19 manuscript revision audit

`paper.tex` now contains the revised attachment, byte for byte. The previous
manuscript is preserved as `manuscripts/paper-2026-09-18.tex`.

| Source | SHA-256 |
|---|---|
| Previous manuscript | `5c87349523a9569fcf74a52435350d54097b6d4a5e2ec57b10f75e5ea209b21d` |
| Revised manuscript | `76592f6d46c40f2632eb426651fa1e062e4427c302a96ad7406b6a923835bb89` |

Both versions contain 20 labeled theorem, lemma, proposition, corollary, or
definition environments. Eighteen statements agree after whitespace
normalization. In `thm:absorption`, “There are” becomes “There exist”; the
mathematical statement is unchanged. In `def:cclass`, the nonempty open set is
called a “region”, nonemptiness of the index set follows from the required
dominant index, and the limit notation explicitly includes n tending to infinity.

## Consequences for the formalization

- All principal theorem conclusions, constants, explicit radii, endpoint
  inequalities, and equivalences remain unchanged. Existing proof terms remain
  applicable.
- The word “region” often includes connectedness. The unchanged sharp
  five-function theorem still allows every nonempty open set. The Lean C-class
  predicate remains defined for arbitrary sets and the relevant theorems retain
  their original open-set hypotheses. Its restriction to regions covers the
  revised definition; connectedness is not added to any unchanged theorem.
- The two-dominant-index discussion and its disconnected Gaussian footnote were
  removed. `CClass.lean` remains a proved auxiliary result, and is no longer
  counted as a separately stated result of the current manuscript.
- The expanded Yamanoi Theorem 1.7 discussion is background, not an added labeled
  author theorem. It is not introduced as an axiom or used to replace a proof.
- The new Wronskian equation labels `eq:wronskian-pointwise-upper` and
  `eq:wronskian-positive-pointwise` describe estimates already proved in
  `WronskianBoundary.lean` and `WronskianMean.lean`. Boundary singularities are
  already handled by circle integrability and the proved negligible error.
- Other changes concern exposition, abstract, keywords, MSC codes, proof and
  section titles, acknowledgments, and bibliography keys. A comment-stripped
  check found no duplicate labels, unresolved ref/eqref targets, or missing
  bibliography keys. This is a LaTeX consistency check, not verification of
  external bibliographic content.

## Working directory

Windows Defender's controlled-folder access started rejecting writes to the
Documents project by Codex, Node, and Git (event 1123, 2026-09-19). The source
tree and Git history were copied, without deletion, to:

`C:/Users/HUAWEI/AppData/Local/ModifiedCartanFormalization/outputs/ModifiedCartan`

Work continues there. The Documents copy remains the preceding snapshot. No
security setting was changed.
